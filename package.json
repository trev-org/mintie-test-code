{
  "name": "mintie-test-code",
  "private": true,
  "version": "0.0.0",
  "workspaces": ["server", "web", "shared"],
  "scripts": {
    "dev:server": "npm --workspace server run dev",
    "dev:web": "npm --workspace web run dev",
    "migrate": "node scripts/migrate.js"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
